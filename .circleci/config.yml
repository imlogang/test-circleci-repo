version: 2.1
jobs:
  build-test:
    docker:
      - image: cimg/node:12.17
    steps:
      - run: echo "run tests"
  package:
    docker:
      - image: cimg/node:12.17
    steps:
      - run: echo "package"
  deploy:
    docker:
      - image: cimg/node:12.17
    steps:
      - run: echo "deploy"

workflows:
  commit:
    jobs:
      - build-test
      
  package:
    jobs:
      - build-test
      - package:
          filters:
            branches:
              ignore: /.*/
            tags:
              only: 
                - test 
                - test1
      - deploy:
          filters:
            branches:
              ignore: /.*/
            tags:
              only: 
                - test 
                - test1
      #test22
            
  deploy:
    jobs:
      - deploy  

